-- История переходов: путь, по которому человек пришёл на текущий экран.
--
-- Хранится у пользователя, а не в user_state: state обнуляется на каждом
-- шаге диалога, а история должна пережить ввод текста. JSON-массив
-- callback_data — последний элемент и есть текущий экран.
ALTER TABLE users ADD COLUMN nav TEXT;
