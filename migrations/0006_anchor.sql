-- 0006_anchor.sql
-- Одно сообщение на весь диалог.
--
-- Бот переписывает единственный экран вместо того, чтобы плодить
-- сообщения. Его идентификатор нужен между запросами, а на Workers
-- памяти между ними нет — значит в базу, рядом с пользователем.
--
-- В user_state класть нельзя: состояние сбрасывается после каждого
-- завершённого мастера, а якорь должен пережить сброс.

ALTER TABLE users ADD COLUMN anchor_msg_id INTEGER;
