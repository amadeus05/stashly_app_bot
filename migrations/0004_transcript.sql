-- 0004_transcript.sql
-- Расшифровка голосовых и аудио.
--
-- Голосовые заметки хранились как файл и подпись — то есть не искались
-- вообще. Текст расшифровки попадает в тот же индекс, что и всё
-- остальное, и «найди, где я говорил про Ван Линя» начинает работать.

ALTER TABLE attachments ADD COLUMN transcript TEXT;

-- Отдельный триггер не нужен: trg_dirty_attachments_au уже помечает
-- вложение на переиндексацию при любом UPDATE.
